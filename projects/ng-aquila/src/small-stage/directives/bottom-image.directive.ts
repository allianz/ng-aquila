import { Directive } from '@angular/core';

/** A directive for passing the bottom image into small stage component. */
@Directive({
    selector: '[nxSmallStageImageBottom]',
})
export class NxSmallStageImageBottomDirective {}
