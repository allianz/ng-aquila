# Tile Group Auto Grid Example

This example demonstrates the `autoGrid` feature of the tile group, which automatically arranges tiles in a responsive grid with a configurable maximum number of columns.
