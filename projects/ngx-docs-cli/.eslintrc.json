{
    "extends": "../../.eslintrc.json",
    "ignorePatterns": ["!**/*"],
    "overrides": [
        {
            "files": ["*.ts"],
            "rules": {
                "@typescript-eslint/prefer-includes": "off" // not supported by tsconfig target of 'es5'
            }
        }
    ]
}
